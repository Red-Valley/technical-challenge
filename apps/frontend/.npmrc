# Configurar npm para usar el registro por defecto
registry=https://registry.npmjs.org/

# Deshabilitar el parcheo automático del lockfile
lockfile-version=2

# Configurar para usar Bun como gestor principal
package-manager=bun 