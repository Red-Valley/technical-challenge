const coreActionRoutes = {
    greet: '/greet',
    create: '/create',
    findAll: '/findAll',
    findOneById: '/findOneById/:id',
    updateOneById: '/updateOneById',
    deleteOneById: '/deleteOneById/:id'
};

export const PATIENT_ROUTES = coreActionRoutes;

export const STATUS_ROUTES = coreActionRoutes;

export const STATUS_HISTORY_ROUTES = coreActionRoutes;

export const PROVIDER_ROUTES = coreActionRoutes;
